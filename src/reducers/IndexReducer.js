const initial = {
	name:'ss',
	age:23
};

function IndexReducer (state = initial,action={}){
	switch(action.type){
		// case 'SEND_TEXT':
		// 	console.log(action.payload)
	}
	return state
}

export default IndexReducer
